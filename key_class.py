class Key(object):
    def __init__(self, location, values, codes, div_name, kb_index, kb_id):
        self.location = location
        self.values = values
        self.codes = codes
        self.div_name = div_name
        self.kb_id = kb_id
        self.kb_index = kb_index

